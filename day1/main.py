'''

Build a todolist application

- Use lists and dictionaries to manage the todo items and their attributes (e.g., status, priority).
- add items to the list
- remove items from the list
- mark items as completed
- set priority for items (low, medium, high)



- for your test cases, have 2 different test files.
- don't put the full functionality in one file, split it into multiple files (e.g., main.py for the application logic and test_todolist.py for the test cases).

8 test cases spanning the below:, test cases:
- Adding an item to the list should increase the total count of items by 1.
- Removing an item from the list should decrease the total count of items by 1.
- Marking an item as completed should change its status to "completed".
- Setting the priority of an item should update its priority level accordingly (throw an exception for invalid priority values).


- Use text fixtures:
- Create a fixture that provides a sample todo list with a few items for testing purposes (2 test fixtures)


- Parametrize test cases:
- Use pytest's parametrize feature to test adding multiple items to the list with different input values (e.g., different item names, priorities).







'''